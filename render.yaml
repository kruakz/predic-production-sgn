services:
  - type: web
    name: prediksi-sgn
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: YOUR_API_KEY
        value: f76a546fc63ea2eb0bec6bbd196c0521
    plan: free
